# String Calculator kata

Test framework: GoogleTest

Build with CMake 3.5

```
mkdir build
cd build
cmake ..
make
```

The build process will download and compile the Google Test libraries from Git. 

To run all tests execute ```make test``` from build folder.

To see the tests in detail go to build/tests folder then execute ```./calculator-test```

